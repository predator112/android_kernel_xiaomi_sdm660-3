--- 
clone: 
  depth: 1
kind: pipeline
name: ReinaPipeline
steps: 
  - 
    commands: 
      - "export token=$BOT_API_KEY"
      - "curl https://raw.githubusercontent.com/Reinazhard/myscripts/llvm/kernel.sh | bash"
    environment: 
      TOKEN: 
        from_secret: BOT_API_KEY
    image: fadlyas/kernel_dockerfile:latest
    name: Azure-Images
    trigger: 
      branch: 
        - unified-test
